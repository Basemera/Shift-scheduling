from django.db import models

from users.models import User

# Create your models here.

# class Shift(models.Model):
#     worker = models.ForeignKey(User, on_delete=models.CASCADE, blank=False, null=False)
#     date_of_shift = models.DateField(auto_created=True, null=False, blank=False)
